# Leídas por app/core/config.py (Settings)
APP_NAME=
DEBUG=
API_PREFIX=
CORS_ORIGINS=
PERSISTENCE_PROVIDER=
# Única variable de conexión (host, puerto, usuario, contraseña, base)
# Ej. local: postgresql+asyncpg://user:pass@localhost:5432/expenses_db
# Ej. Railway: la que inyecta el plugin Postgres
DATABASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=
LOG_LEVEL=

# entrypoint.sh: espera TCP antes de migraciones (segundos)
DB_WAIT_TIMEOUT=
